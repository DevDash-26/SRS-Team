# Campus Hub — Frontend

React + Vite client for the UCL Campus Hub.
See /FEATURES_MAP.md in the project root for how each business requirement maps to files here.

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and fill in real values
3. `npm run dev`
